#include <stdio.h>

int strlen(char *string)
{
	int length = 0;
	
	while(*string++ != '\:w!
0')
		length += 1;

	return length;
}


void main()
{
	char str[] = {"abcdefg0"};

	int len = 0;
	
	len = strlen(str);

	printf("%d\n",len);
}
